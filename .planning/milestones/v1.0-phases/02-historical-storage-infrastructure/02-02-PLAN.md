---
phase: 02-historical-storage-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - delay_consumer.py
  - api.py
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "Delay observations are successfully persisted to TimescaleDB"
    - "Vehicle positions are stored in SQL instead of MongoDB"
    - "API returns vehicle data from SQL"
  artifacts:
    - path: "delay_consumer.py"
      provides: "Batch-enabled SQL persistence"
    - path: "api.py"
      provides: "SQL-backed REST API"
  key_links:
    - from: "delay_consumer.py"
      to: "TimescaleDB"
      via: "asyncpg Batch Insert"
---

<objective>
Update the consumer to batch persist observations to SQL and switch the API to use SQL.

Purpose: High-performance data persistence and retrieval.
Output: Fully operational SQL-backed data pipeline.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-historical-storage-infrastructure/02-01-PLAN.md
@.planning/phases/02-historical-storage-infrastructure/02-RESEARCH.md
@delay_consumer.py
@api.py
</context>

<tasks>

<task type="auto">
  <name>Migrate Consumer to SQL Batching</name>
  <files>delay_consumer.py</files>
  <read_first>delay_consumer.py, .planning/phases/02-historical-storage-infrastructure/02-RESEARCH.md</read_first>
  <action>
    - Update `delay_consumer.py` to use `asyncpg`.
    - Implement an in-memory buffer for `delay_observations`.
    - Use `conn.copy_records_to_table` to flush observations when buffer >= 100 or every 10 seconds.
    - Update vehicle position logic to UPSERT into the `active_vehicles` table instead of MongoDB.
    - Per D-07, remove MongoDB persistence logic from this consumer.
  </action>
  <verify>
    <automated>python delay_consumer.py --test-run && docker compose exec timescale psql -U postgres -d transit -c "SELECT count(*) FROM delay_observations"</automated>
  </verify>
  <done>Consumer persists to SQL with batching.</done>
  <acceptance_criteria>
    - delay_consumer.py uses asyncpg
    - No references to pymongo in delay_consumer.py
    - `copy_records_to_table` is used for inserts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Update API to SQL Backend</name>
  <files>api.py</files>
  <read_first>api.py</read_first>
  <action>
    - Replace `AsyncMongoClient` with `asyncpg` pool in `api.py`.
    - Update `/api/vehicles/` route to query the `active_vehicles` SQL table.
    - Ensure Pydantic models still match the SQL result structure.
    - Clean up unused MongoDB connection code.
  </action>
  <verify>
    <automated>curl http://localhost:8000/api/vehicles/</automated>
  </verify>
  <done>API serves data from TimescaleDB.</done>
  <acceptance_criteria>
    - api.py uses asyncpg pool
    - SQL query used for vehicle retrieval
    - MongoDB code removed
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | HTTP requests to SQL-backed API |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Consumer flushes | mitigate | Ensure DB transactions are used for batch flushes to maintain integrity. |
</threat_model>

<verification>
Start consumer, wait for batch flush, then query API to see if vehicles appear.
</verification>

<success_criteria>
- Zero records written to MongoDB.
- Continuous delay observations appearing in `delay_observations` table.
- API returning valid vehicle data from `active_vehicles`.
</success_criteria>

<output>
After completion, create .planning/phases/02-historical-storage-infrastructure/02-02-SUMMARY.md
</output>
