---
phase: 02-historical-storage-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - pyproject.toml
  - db_init.py
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "TimescaleDB container is running and healthy"
    - "PostgreSQL driver asyncpg is installed"
    - "Schema for delay observations and active vehicles is initialized"
  artifacts:
    - path: "docker-compose.yml"
      provides: "TimescaleDB service definition"
    - path: "db_init.py"
      provides: "Database initialization script"
  key_links:
    - from: "db_init.py"
      to: "TimescaleDB"
      via: "SQL connection"
---

<objective>
Set up the TimescaleDB infrastructure and initialize the SQL schema to replace MongoDB.

Purpose: Provide a scalable, time-series optimized foundation for transit data.
Output: Working TimescaleDB service with hypertables created.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-historical-storage-infrastructure/02-CONTEXT.md
@.planning/phases/02-historical-storage-infrastructure/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Update Infrastructure and Dependencies</name>
  <files>docker-compose.yml, pyproject.toml</files>
  <read_first>docker-compose.yml, pyproject.toml</read_first>
  <action>
    - Add TimescaleDB service to `docker-compose.yml` using image `timescale/timescaledb:latest-pg17`.
    - Configure environment variables for DB credentials (POSTGRES_USER, POSTGRES_PASSWORD).
    - Map port 5432 to host.
    - Add `asyncpg` and `SQLAlchemy` to `pyproject.toml` dependencies.
    - Run `uv sync` to update environment.
  </action>
  <verify>
    <automated>docker compose ps timescale | grep Up && uv lock --check</automated>
  </verify>
  <done>TimescaleDB is running and asyncpg is installed.</done>
  <acceptance_criteria>
    - docker-compose.yml contains 'timescale/timescaledb'
    - pyproject.toml contains 'asyncpg'
    - `uv sync` completes successfully
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Initialize Database Schema</name>
  <files>db_init.py</files>
  <read_first>.planning/phases/02-historical-storage-infrastructure/02-RESEARCH.md</read_first>
  <action>
    - Create `db_init.py` that connects to TimescaleDB.
    - Implement SQL to create `active_vehicles` table (columns: vehicle_id, route_id, trip_id, latitude, longitude, updated_at).
    - Implement SQL to create `delay_observations` table (columns: observed_at, stop_id, route_id, trip_id, delay_seconds).
    - Convert `delay_observations` into a hypertable using `SELECT create_hypertable('delay_observations', 'observed_at');`.
    - Set up a continuous aggregate for hourly delay statistics as defined in RESEARCH.md.
  </action>
  <verify>
    <automated>python db_init.py && docker compose exec timescale psql -U postgres -d transit -c "\dt"</automated>
  </verify>
  <done>Schema is created and hypertable is initialized.</done>
  <acceptance_criteria>
    - Table 'active_vehicles' exists
    - Table 'delay_observations' exists
    - `_timescaledb_catalog.hypertable` contains 'delay_observations'
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App -> DB | Database credentials used for connection |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | SQL Queries | mitigate | Use parameterized queries via asyncpg to prevent SQL injection. |
| T-02-02 | Information Disclosure | .env/config | mitigate | Ensure DB credentials are not hardcoded and .env is ignored. |
</threat_model>

<verification>
Run `python db_init.py` and verify table existence via psql.
</verification>

<success_criteria>
- TimescaleDB container is running.
- All tables and hypertables exist in the 'transit' database.
</success_criteria>

<output>
After completion, create .planning/phases/02-historical-storage-infrastructure/02-01-SUMMARY.md
</output>
