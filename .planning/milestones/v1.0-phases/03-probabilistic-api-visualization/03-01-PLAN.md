---
phase: 03-probabilistic-api-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api.py, pyproject.toml]
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "API returns histogram data for a stop"
    - "Typical delay (median) is calculated correctly"
  artifacts:
    - path: "api.py"
      provides: "New /api/distribution/{stop_id} endpoint"
---

<objective>
Implement the probabilistic API endpoint that calculates delay distributions and median statistics from historical data.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add statistical dependencies</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml</read_first>
  <action>Ensure numpy and any other required statistical libs are in pyproject.toml. (NumPy already present, verify version and sync if needed).</action>
  <acceptance_criteria>
    - pyproject.toml contains numpy
  </acceptance_criteria>
  <verify>uv sync</verify>
  <done>Dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Implement distribution endpoint</name>
  <files>api.py</files>
  <read_first>api.py, db_init.py</read_first>
  <action>
    Add a new GET endpoint /api/distribution/{stop_id}.
    1. Query delay_observations for the given stop.
    2. Filter by a 2-hour window centered on current time.
    3. Categorize by DayType (Weekday/Weekend).
    4. Calculate 1-minute histogram buckets using NumPy or SQL.
    5. Calculate the median delay.
    6. Return JSON: { "stop_id": string, "median": number, "buckets": [{ "minute": number, "count": number }] }
  </action>
  <acceptance_criteria>
    - api.py has @app.get("/api/distribution/{stop_id}")
    - Returns JSON with 'median' and 'buckets' keys
  </acceptance_criteria>
  <verify>python -m pytest tests/test_api_distribution.py (create scaffold if needed)</verify>
  <done>API returns distribution data</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | /api/distribution | mitigate | Only return aggregated bucket data, no raw observation timestamps or trip IDs |
</threat_model>

<success_criteria>
- GET /api/distribution/123 returns 200 with histogram JSON.
</success_criteria>
