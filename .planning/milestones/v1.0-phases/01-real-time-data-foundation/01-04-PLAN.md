---
phase: 01-real-time-data-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-03]
files_modified: [frontend/src/stores/filterStore.ts, frontend/src/components/Map.tsx, frontend/src/api/database.ts]
autonomous: true
gap_closure: true
requirements: [CORE-01, CORE-02, CORE-03]
must_haves:
  truths:
    - "User can select a stop on the map."
    - "The frontend displays a real-time 'Minutes Away' countdown for the selected stop."
  artifacts:
    - path: "frontend/src/stores/filterStore.ts"
      provides: "selectedStop state"
    - path: "frontend/src/components/Map.tsx"
      provides: "Stop markers and countdown UI"
---

<objective>
Implement stop selection, stop markers, and real-time arrival countdowns in the frontend.

Purpose: Close the major gaps identified in Phase 1 verification regarding CORE-01 and CORE-02.
Output: A functional UI where users can click stops to see arrival estimates.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/turq/code/transit-data/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-real-time-data-foundation/01-VERIFICATION.md
@.planning/phases/01-real-time-data-foundation/01-CONTEXT.md
@frontend/src/stores/filterStore.ts
@frontend/src/components/Map.tsx
@frontend/src/api/database.ts

<interfaces>
From filterStore.ts:
```typescript
export const useFilterStore = create<FilterStore>((set) => ({
  routeFilter: "",
  setRouteFilter: (route) => set({ routeFilter: route }),
  // Need to add selectedStop
}));
```

From Map.tsx:
```typescript
// Currently renders vehicles. Needs to render stops and handle clicks.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend state and types</name>
  <files>frontend/src/stores/filterStore.ts, frontend/src/api/database.ts</files>
  <read_first>frontend/src/stores/filterStore.ts, frontend/src/api/database.ts</read_first>
  <action>
    1. Update `frontend/src/api/database.ts` to include `next_stop_id` in the `Vehicle` interface.
    2. Add `selectedStopId: string | null` and `setSelectedStopId: (id: string | null) => void` to `FilterState` and `FilterActions` in `frontend/src/stores/filterStore.ts`.
  </action>
  <verify>
    <automated>grep "selectedStopId" frontend/src/stores/filterStore.ts</automated>
  </verify>
  <done>Zustand store manages selected stop state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Stop Markers and Countdown Popups</name>
  <files>frontend/src/components/Map.tsx</files>
  <read_first>frontend/src/components/Map.tsx</read_first>
  <action>
    1. Update `Map.tsx` to render stops (from a static list or API if available, otherwise use a subset of known stop IDs for testing).
    2. Add click handlers to stops that set the `selectedStopId` in the store.
    3. Modify vehicle rendering: when a stop is selected, filter or highlight vehicles where `next_stop_id === selectedStopId`.
    4. In the stop popup (per D-05), calculate and display "Minutes Away" based on the `delay_seconds` and scheduled time (if available) or simply display the relative delay status.
  </action>
  <verify>
    <automated>grep "Popup" frontend/src/components/Map.tsx</automated>
  </verify>
  <done>Map displays stops, allowing selection and showing arrival info.</done>
</task>

</tasks>

<verification>
Start the frontend and verify that markers for stops appear on the map.
Click a stop and verify a popup appears with arrival info.
Confirm that selecting a stop updates the global filter state.
</verification>

<success_criteria>
CORE-01 and CORE-02 are satisfied: user can select a stop and see countdowns.
</success_criteria>

<output>
After completion, create `.planning/phases/01-real-time-data-foundation/01-04-SUMMARY.md`
</output>
