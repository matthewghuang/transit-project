---
phase: 01-real-time-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [main.py, delay_consumer.py]
autonomous: true
requirements: [REL-01, CORE-01, CORE-02]
must_haves:
  truths:
    - "Kafka producer publishes TripUpdate messages to trip_updates topic"
    - "delay_consumer.py loads stop_times.txt into memory"
    - "delay_consumer.py persists delay metrics to MongoDB"
  artifacts:
    - path: "delay_consumer.py"
      provides: "Real-time delay calculation engine"
    - path: "main.py"
      provides: "Multi-feed GTFS-R producer"
---

<objective>
Implement the backend ingestion pipeline for GTFS-Realtime TripUpdates and calculate vehicle delays by joining with static schedules.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-real-time-data-foundation/01-CONTEXT.md
@.planning/phases/01-real-time-data-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update main.py to produce TripUpdates</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <acceptance_criteria>
    - main.py contains logic to poll `realtime_url`
    - main.py publishes to `trip_updates` Kafka topic
    - FeedMessage parsing handles both position and trip update feeds
  </acceptance_criteria>
  <action>
    Modify main.py to poll both the position and trip update endpoints (defined as `realtime_url` and `position_url`).
    Ensure the producer publishes TripUpdate entities to a new Kafka topic named `trip_updates`.
    Reuse the existing `entity_cache` pattern if applicable.
  </action>
  <verify>
    <automated>python3 main.py --dry-run | grep "trip_updates"</automated>
  </verify>
  <done>Producer now broadcasts TripUpdate data.</done>
</task>

<task type="auto">
  <name>Task 2: Create delay_consumer.py with in-memory schedule</name>
  <files>delay_consumer.py</files>
  <read_first>demo_consumer.py, google_transit/stop_times.txt</read_first>
  <acceptance_criteria>
    - delay_consumer.py loads `stop_times.txt` using pandas per D-02
    - Data structure is `{(trip_id, stop_id): scheduled_arrival_time}` per D-01
    - Calculates lateness using `header.timestamp` as anchor per D-04
    - Persists updates to MongoDB `delays` collection
  </acceptance_criteria>
  <action>
    Create `delay_consumer.py` based on the pattern in `demo_consumer.py`.
    On startup, load `google_transit/stop_times.txt` into a dictionary for O(1) lookup.
    Listen to the `trip_updates` topic.
    For each update, join with the schedule to calculate delay in seconds.
    Implement "Strict Mode" (D-03): only record if `stop_time_update` is present.
    Write the resulting `delay_seconds` and `stop_id` to MongoDB.
  </action>
  <verify>
    <automated>python3 -m py_compile delay_consumer.py</automated>
  </verify>
  <done>Delay metrics are being calculated and persisted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Translink API → main.py | External data ingestion |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | main.py | mitigate | Validate GTFS-R Protobuf signatures during parsing |
</threat_model>

<success_criteria>
Backend can process real-time delays from feed into MongoDB.
</success_criteria>
