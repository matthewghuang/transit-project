---
phase: 01-real-time-data-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [delay_consumer.py, api.py]
autonomous: true
gap_closure: true
requirements: [REL-01]
must_haves:
  truths:
    - "Kafka consumer correctly identifies the 'next stop' for a vehicle and calculates lateness."
  artifacts:
    - path: "delay_consumer.py"
      provides: "Next-stop focused delay calculation"
  key_links:
    - from: "delay_consumer.py"
      to: "MongoDB (delays)"
      via: "collection.update_one"
---

<objective>
Refine the backend delay calculation logic in `delay_consumer.py` to explicitly identify and persist the "next stop" arrival/departure prediction for each vehicle, rather than just calculating delays for all stop time updates.

Purpose: Fix the partial verification of REL-01 where delay semantics were too broad.
Output: Updated `delay_consumer.py` that isolates the logical next stop.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/turq/code/transit-data/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-real-time-data-foundation/01-VERIFICATION.md
@.planning/phases/01-real-time-data-foundation/01-RESEARCH.md
@delay_consumer.py
@api.py

<interfaces>
From delay_consumer.py:
```python
# Current logic calculates delays for all updates:
for update in entity.trip_update.stop_time_update:
    # ... calculates delay ...
```

From api.py:
```python
class VehicleDetails(BaseModel):
    # ...
    delay_seconds: Optional[int] = None
    next_stop_id: Optional[str] = None # Proposed addition
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update delay_consumer.py to isolate next stop</name>
  <files>delay_consumer.py</files>
  <read_first>delay_consumer.py</read_first>
  <action>
    Modify the processing loop in `delay_consumer.py` to identify the most relevant stop_time_update (the one with the lowest non-negative stop_sequence or the one matching the vehicle's current stop_id if available).
    Calculate the delay specifically for this "next stop" and include `next_stop_id` in the document persisted to MongoDB.
    
    Logic:
    1. Sort `stop_time_update` by `stop_sequence`.
    2. Pick the first update that has not been passed yet (check against `current_stop_sequence` from `VehiclePosition` if cross-topic joining is enabled, or simply pick the first update in the `trip_update` list if ordered by agency).
    3. Update the MongoDB `delays` collection with `{"_id": vehicle_id, "delay_seconds": X, "next_stop_id": Y, "timestamp": Z}`.
  </action>
  <verify>
    <automated>grep "next_stop_id" delay_consumer.py</automated>
  </verify>
  <done>Consumer persists next_stop_id along with delay_seconds.</done>
</task>

<task type="auto">
  <name>Task 2: Expose next_stop_id in API</name>
  <files>api.py</files>
  <read_first>api.py</read_first>
  <action>
    Update the `VehicleDetails` Pydantic model in `api.py` to include an optional `next_stop_id: str` field.
    Update the `/api/vehicles/` route logic to pull the `next_stop_id` from the delay observation in MongoDB.
  </action>
  <verify>
    <automated>grep "next_stop_id" api.py</automated>
  </verify>
  <done>API returns next_stop_id for vehicles with calculated delays.</done>
</task>

</tasks>

<verification>
Run `delay_consumer.py` and verify MongoDB entries contain `next_stop_id`.
Query `GET /api/vehicles/` and confirm `next_stop_id` is present in the response.
</verification>

<success_criteria>
Backend identifies the logical next stop for each vehicle and exposes it via API.
</success_criteria>

<output>
After completion, create `.planning/phases/01-real-time-data-foundation/01-03-SUMMARY.md`
</output>
