---
phase: 01-real-time-data-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [api.py, frontend/src/components/Map.tsx, frontend/src/hooks/usePositions.ts]
autonomous: true
requirements: [CORE-03]
must_haves:
  truths:
    - "API returns delay_seconds for vehicles"
    - "Map popup shows lateness in minutes"
  artifacts:
    - path: "api.py"
      provides: "Delay-aware REST endpoints"
    - path: "frontend/src/components/Map.tsx"
      provides: "Stop-focused real-time UI"
---

<objective>
Update the API and Frontend to expose and visualize the calculated real-time delays.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-real-time-data-foundation/01-CONTEXT.md
@.planning/phases/01-real-time-data-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API to include delay information</name>
  <files>api.py</files>
  <read_first>api.py</read_first>
  <acceptance_criteria>
    - Pydantic models in api.py include `delay_seconds`
    - GET /api/vehicles/ returns delay data if available
  </acceptance_criteria>
  <action>
    Modify the `VehicleUpdate` Pydantic model in `api.py` to include an optional `delay_seconds` field.
    Update the MongoDB query logic to fetch enriched vehicle data from the relevant collection(s).
  </action>
  <verify>
    <automated>curl -s http://localhost:8000/api/vehicles/ | grep "delay_seconds"</automated>
  </verify>
  <done>API now serves delay metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Map Popups with Lateness (D-05)</name>
  <files>frontend/src/components/Map.tsx</files>
  <read_first>frontend/src/components/Map.tsx</read_first>
  <acceptance_criteria>
    - Marker Popups display "X minutes late"
    - Popup styling matches minimal aesthetic
  </acceptance_criteria>
  <action>
    Update the `Map.tsx` component to include delay information in the vehicle markers' `Popup`.
    If `delay_seconds` is positive, show "X minutes late". If negative, show "X minutes early".
    Follow D-05 to ensure the popup is the primary anchor for this info.
  </action>
  <verify>
    <automated>grep -i "late" frontend/src/components/Map.tsx</automated>
  </verify>
  <done>Frontend visualizes real-time lateness.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | api.py | accept | Only public transit data exposed |
</threat_model>

<success_criteria>
Users can see real-time lateness for vehicles on the map.
</success_criteria>
