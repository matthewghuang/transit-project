---
phase: 03-probabilistic-api-visualization
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [frontend/package.json, frontend/src/components/DelayDistributionChart.tsx, frontend/src/components/Map.tsx]
autonomous: false
requirements: [REL-04]
must_haves:
  truths:
    - "Users see an area chart of delay probabilities"
    - "Chart is visible in the vehicle/stop context"
  artifacts:
    - path: "frontend/src/components/DelayDistributionChart.tsx"
      provides: "Area chart visualization"
---

<objective>
Implement the frontend visualization of the delay distribution using an area chart.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add charting library</name>
  <files>frontend/package.json</files>
  <read_first>frontend/package.json</read_first>
  <action>Add recharts to frontend/package.json.</action>
  <acceptance_criteria>
    - frontend/package.json contains "recharts"
  </acceptance_criteria>
  <verify>npm install --prefix frontend</verify>
  <done>Recharts installed</done>
</task>

<task type="auto">
  <name>Task 2: Create DelayDistributionChart component</name>
  <files>frontend/src/components/DelayDistributionChart.tsx</files>
  <read_first>frontend/src/components/Map.tsx</read_first>
  <action>
    Create a new React component using Recharts.
    - Props: stop_id
    - Fetch data from /api/distribution/{stop_id}
    - Render an AreaChart with 'minute' on X-axis and 'count' on Y-axis.
    - Style with area shading (D-04).
    - Display the median value as a summary label (D-06).
  </action>
  <acceptance_criteria>
    - File exists: frontend/src/components/DelayDistributionChart.tsx
    - Uses AreaChart from recharts
  </acceptance_criteria>
  <verify>npm run build --prefix frontend (visual check in next task)</verify>
  <done>Chart component implemented</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify Chart Visualization</name>
  <action>Start the environment and visually inspect the chart in the browser.</action>
  <what-built>Area chart visualization in the dashboard</what-built>
  <how-to-verify>
    1. Start the app (docker compose up).
    2. Open dashboard (typically http://localhost:1234).
    3. Select a stop or vehicle.
    4. Verify the distribution chart appears and looks like a shaded area curve.
  </how-to-verify>
  <verify>Manual visual check</verify>
  <done>Visual verification complete</done>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Cross-Site Scripting (XSS) | Chart Tooltips | mitigate | Use Recharts default tooltip rendering which escapes data values |
</threat_model>

<success_criteria>
- Dashboard displays a shaded area chart for delay distributions.
</success_criteria>
