---
phase: 03-probabilistic-api-visualization
plan: 03
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified: [docker-compose.yml, db_init.py]
autonomous: false
requirements: [REL-02, REL-03]
must_haves:
  truths:
    - "Database schema is automatically initialized on startup"
    - "API and Consumer no longer fail with 'relation does not exist' errors"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Automated DB initialization service"
    - path: "db_init.py"
      provides: "Schema verification and creation"
---

<objective>
Resolve the 'relation "delay_observations" does not exist' error by integrating the database initialization script into the automated startup flow using Docker Compose.

Purpose: Ensure the TimescaleDB schema is always present, even after a cold start or database volume wipe, which is critical for the probabilistic API and visualization.
Output: A self-healing infrastructure where the database is initialized before dependent services start.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-probabilistic-api-visualization/03-UAT.md
@docker-compose.yml
@db_init.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add db-init service to Docker Compose</name>
  <files>docker-compose.yml</files>
  <action>
    Add a new service named `db-init` to `docker-compose.yml`.
    - It should use the same image as the backend (built from the local Dockerfile).
    - It should run `python db_init.py`.
    - It should depend on the `timescale` service.
    - Set restart policy to `on-failure`.
    - Map the same environment variables as the `timescale` service.
    
    Additionally, update any services that depend on the database (like a backend API or consumer if they were in compose) to also depend on `db-init` with `condition: service_completed_successfully`.
  </action>
  <verify>
    <automated>docker compose config | grep db-init</automated>
  </verify>
  <done>db-init service defined in docker-compose.yml</done>
</task>

<task type="auto">
  <name>Task 2: Enhance db_init.py with wait-for-it logic</name>
  <files>db_init.py</files>
  <action>
    Modify `db_init.py` to include a retry loop or wait logic to ensure the TimescaleDB service is actually ready to accept connections before attempting initialization. This prevents the init script from failing if the DB container is still booting.
    
    Implementation details:
    - Add a `wait_for_db` function that tries to connect to the `postgres` database with a timeout/retry limit (e.g., 5 retries with 5s delay).
    - Use `asyncpg.connect` to test connectivity.
  </action>
  <verify>
    <automated>python3 db_init.py --help || true (Check for syntax errors)</automated>
  </verify>
  <done>db_init.py is resilient to DB startup delays</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify automated initialization</name>
  <what-built>Automated database initialization</what-built>
  <how-to-verify>
    1. Stop all containers: `docker compose down -v` (The -v is critical to wipe the DB volume).
    2. Start services: `docker compose up -d`.
    3. Monitor the init logs: `docker compose logs -f db-init`.
    4. Verify the table exists: `docker exec -it $(docker ps -q -f name=timescale) psql -U postgres -d transit -c "\dt delay_observations"`.
    5. Verify the API no longer returns 500: `curl http://localhost:8000/api/distribution/some_stop_id`.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Init Service → TimescaleDB | Internal service-to-service communication |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | db_init.py | mitigate | Ensure the init service only runs once or uses 'IF NOT EXISTS' to prevent data loss on restarts. |
</threat_model>

<verification>
Check that `docker compose up` results in a fully initialized database schema without manual intervention.
</verification>

<success_criteria>
1. `docker compose up` automatically creates the `transit` database and `delay_observations` table.
2. The `api.py` endpoint `/api/distribution/{stop_id}` returns a 200 (even if empty) instead of a 500 error about missing relations.
</success_criteria>

<output>
After completion, create `.planning/phases/03-probabilistic-api-visualization/03-03-SUMMARY.md`
</output>
