---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/stores/filterStore.ts, frontend/src/components/DelayDistributionChart.tsx, frontend/src/components/TimeTriad.tsx]
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]

must_haves:
  truths:
    - "User can adjust confidence level via a slider"
    - "Delay distribution chart highlights the area under the curve corresponding to selected confidence"
    - "Arrive-by time updates instantly as the slider moves"
  artifacts:
    - path: "frontend/src/stores/filterStore.ts"
      provides: "Global confidence state"
    - path: "frontend/src/components/DelayDistributionChart.tsx"
      provides: "Dynamic shading of probability mass"
    - path: "frontend/src/components/TimeTriad.tsx"
      provides: "Confidence slider and zero-latency arrive-by updates"
  key_links:
    - from: "frontend/src/components/TimeTriad.tsx"
      to: "frontend/src/stores/filterStore.ts"
      via: "useFilterStore hook"
    - from: "frontend/src/components/DelayDistributionChart.tsx"
      to: "frontend/src/stores/filterStore.ts"
      via: "useFilterStore hook"
---

<objective>
Implement the interactive confidence UI, allowing users to adjust their reliability threshold via a slider and see real-time updates on both the "Arrive-by" time and the delay distribution visualization.

Purpose: Empower commuters to make personalized risk-aware decisions by visualizing the tradeoff between time and reliability.
Output: Updated TimeTriad and DelayDistributionChart components with slider-driven dynamic feedback.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/turq/code/transit-data/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-interactive-confidence-ui/07-CONTEXT.md
@.planning/phases/07-interactive-confidence-ui/07-UI-SPEC.md
</context>

<interfaces>
From frontend/src/stores/filterStore.ts:
```typescript
export const useFilterStore = create<FilterStore>((set) => ({
  selectedStopId: string | null;
  setSelectedStopId: (id: string | null) => void;
}));
```

From frontend/src/components/DelayDistributionChart.tsx:
```typescript
interface Bucket {
  minute: number;
  count: number;
}
interface DistributionData {
  buckets: Bucket[];
  // ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add confidenceLevel to filterStore</name>
  <files>frontend/src/stores/filterStore.ts</files>
  <action>
    - Add `confidenceLevel: number` to `FilterState` (default: 95).
    - Add `setConfidenceLevel: (level: number) => void` to `FilterActions`.
    - Implement the setter in `useFilterStore`.
  </action>
  <verify>
    <automated>grep "confidenceLevel" frontend/src/stores/filterStore.ts</automated>
  </verify>
  <done>Global state supports tracking and updating user confidence preference.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dynamic shading in DelayDistributionChart</name>
  <files>frontend/src/components/DelayDistributionChart.tsx</files>
  <action>
    - Consume `confidenceLevel` from `useFilterStore`.
    - Implement local percentile calculation per UI-SPEC (Method: iterate sorted buckets to find cutoff minute where cumulative count >= percentile * total).
    - Update Recharts implementation:
      - Keep existing "Base" Area with low opacity.
      - Add a second "Confidence" Area using `buckets.filter(b => b.minute <= cutoff)` or similar logic (e.g., mapping data to include a `shadedCount` property).
      - Style shading using `rgba(37, 99, 235, 0.2)` per UI-SPEC.
  </action>
  <verify>
    <automated>grep "useFilterStore" frontend/src/components/DelayDistributionChart.tsx && grep "Area" frontend/src/components/DelayDistributionChart.tsx | wc -l | grep 2</automated>
  </verify>
  <done>Chart visually represents the probability mass for the selected confidence level.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Slider and Instant Arrive-By in TimeTriad</name>
  <files>frontend/src/components/TimeTriad.tsx</files>
  <action>
    - Add a reliability slider within the `triad-details` section (visible when expanded).
    - Slider range: 50% to 99%, steps: [50, 75, 90, 95, 99] per D-06/UI-SPEC.
    - Connect slider to `useFilterStore.setConfidenceLevel`.
    - Implement zero-latency "Arrive-By" calculation:
      - If distribution data is available (can pass down from parent or share query), calculate $ArriveBy_{est} = ScheduledTime - Delay_{cutoff}$.
      - Otherwise, use the server-provided `data.arrive_by_time` (fallback).
    - Display updated confidence label (e.g. "{percent}% confidence").
  </action>
  <verify>
    <automated>grep "setConfidenceLevel" frontend/src/components/TimeTriad.tsx</automated>
  </verify>
  <done>User can interactively adjust reliability and see immediate impact on arrival advice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client Component → Store | State updates must remain within valid range [50-99] |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | filterStore | mitigate | Ensure slider only emits discrete valid steps; validate range if persisted. |
</threat_model>

<verification>
1. Open dashboard and search for a stop.
2. Click the stop to expand the Time Triad.
3. Move the reliability slider and verify:
   - Arrive-by time updates instantly.
   - Delay chart shading expands/contracts to match the slider percentile.
   - Text label shows the correct percentage certainty.
</verification>

<success_criteria>
- Interactive slider controls the threshold.
- Chart shading accurately represents the probability mass.
- Arrive-by updates are perceived as instantaneous by the user.
</success_criteria>

<output>
After completion, create `.planning/phases/07-interactive-confidence-ui/07-01-SUMMARY.md`
</output>
