---
phase: 04-search-first-entry-time-comparisons
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [api.py, frontend/src/components/HeroSearch.tsx, frontend/src/components/TimeTriad.tsx, frontend/src/App.css]
autonomous: true
requirements: [SRCH-02, UIO-01, UIO-04]
gap_closure: true

must_haves:
  truths:
    - "Entering a 5-digit stop ID (e.g., 50959) returns the correct stop."
    - "Time Triad is centered in the dashboard."
    - "Hero time section does not show 'prediction' subtitle."
    - "Expanded view shows Scheduled, Actual, and Predicted times clearly."
  artifacts:
    - path: "api.py"
      provides: "Fix for stop ID truncation in search"
    - path: "frontend/src/App.css"
      provides: "Centered dashboard layout"
    - path: "frontend/src/components/TimeTriad.tsx"
      provides: "Cleanup of labels and fix for missing times in expanded view"
  key_links:
    - from: "HeroSearch.tsx"
      to: "/api/stops/search"
      via: "fetch"
    - from: "TimeTriad.tsx"
      to: "triad-grid"
      via: "conditional rendering"
---

<objective>
Fix remaining Phase 4 gaps identified in UAT:
1. Fix stop ID "50959" appearing as "#959" and not being searchable.
2. Center the Time Triad and remove the "prediction" subtitle.
3. Fix "Actual" time missing from the expanded triad view.

Purpose: Ensure high-intent search works correctly for numeric IDs and the UI is polished for mobile use.
Output: Working numeric search and centered, complete time comparison UI.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/turq/code/transit-data/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-search-first-entry-time-comparisons/04-UAT.md
@api.py
@frontend/src/components/HeroSearch.tsx
@frontend/src/components/TimeTriad.tsx
@frontend/src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Stop ID Numeric Search & Display</name>
  <files>api.py, frontend/src/components/HeroSearch.tsx</files>
  <action>
    - In `api.py`: The issue likely stems from `is_numeric` check or the SQL query. Ensure `q` is handled as a string. However, the user reports "50959" appearing as "#959". This suggests the *data* in the `stops` table might be truncated or the frontend is stripping characters.
    - Check `HeroSearch.tsx` line 97: `<span className="stop-id">#{stop.id}</span>`. If `stop.id` is "959", the backend is returning "959".
    - In `api.py`: Review `search_stops`. The query `WHERE s.stop_id = $1 OR s.stop_name % $1` is correct for strings.
    - Potential Root Cause: If the `stops` table was populated from a CSV where `stop_id` was treated as an integer by an import tool, leading digits could be lost if they were zeros, but 50959 has no leading zeros. If it shows "959", maybe it was treated as a short int?
    - Action: Modify `api.py` search logic to also try partial matches on stop_id if exact fails, but primarily check why "50959" might be stored as "959". 
    - Since I cannot re-import data easily, I will add a fallback in `api.py` search: `WHERE s.stop_id LIKE '%' || $1 OR ...`. But wait, "50959" is longer than "959". If the user types "50959" and it is stored as "959", `s.stop_id = $1` fails.
    - Fix: In `api.py`, ensure we are searching stop_id as a string and consider if any casting is happening.
    - Update `HeroSearch.tsx`: Ensure the display of `#stop.id` handles the string correctly.
  </action>
  <verify>
    <automated>curl "http://localhost:8000/api/stops/search?q=50959"</automated>
  </verify>
  <done>Search for "50959" returns the stop, and it displays as "#50959" (or the stored ID).</done>
</task>

<task type="auto">
  <name>Task 2: Center Dashboard & Clean Triad UI</name>
  <files>frontend/src/App.css, frontend/src/components/TimeTriad.tsx</files>
  <action>
    - In `App.css`: Ensure `.dashboard-main` and `.hero-time-section` are correctly centered. `.dashboard-main` has `margin: 0 auto` and `max-width: 800px`, but maybe the parent `main` needs `display: block` or better alignment when map is removed.
    - In `TimeTriad.tsx`: Remove the `hero-status` div or the text inside it if it says "Prediction". The UAT says "It shouldn't show prediction under the time." 
    - Remove lines 40-42 in `TimeTriad.tsx`.
  </action>
  <verify>
    <automated>grep -v "hero-status" frontend/src/components/TimeTriad.tsx</automated>
  </verify>
  <done>Dashboard is centered and 'Prediction' label is removed from hero section.</done>
</task>

<task type="auto">
  <name>Task 3: Restore Actual Time in Expanded View</name>
  <files>frontend/src/components/TimeTriad.tsx</files>
  <behavior>
    - When `isExpanded` is true, the `triad-grid` must display Scheduled, Actual, and Predicted times.
    - Currently, `times` array is defined at line 17.
    - The `triad-grid` at line 47 maps over `times`.
    - User reports "actual time is missing". This might be because `data.actual_time` is null or the CSS is hiding it.
    - Verify `actual_time` is correctly passed from `useNextBuses`.
    - Ensure `triad-col` for Actual is not conditionally hidden.
  </behavior>
  <action>
    - Review `TimeTriad.tsx`. Ensure `data.actual_time` is being used correctly in the `times` array.
    - Add a fallback display like "Not available" if `actual_time` is missing, but ensure the column itself exists.
  </action>
  <verify>
    <automated>grep "Actual" frontend/src/components/TimeTriad.tsx</automated>
  </verify>
  <done>Actual time column is visible in the expanded triad view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Search query `q` is untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Injection | api.py (search_stops) | mitigate | Uses asyncpg parameterized queries ($1) which prevents SQL injection. |
</threat_model>

<verification>
- Stop ID search works for 50959.
- Dashboard UI is centered.
- Expanded triad shows all 3 time types.
</verification>

<success_criteria>
- Search returns 50959.
- Dashboard looks balanced/centered.
- No 'Prediction' text in hero.
- Actual time visible in details.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-first-entry-time-comparisons/04-05-SUMMARY.md`
</output>
