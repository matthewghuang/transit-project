---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [api.py, db_init.py]
autonomous: true
requirements: [SRCH-03, SRCH-02]
must_haves:
  truths:
    - "Search API returns relevant stops for fuzzy queries"
    - "Search API returns specific stop for exact ID"
  artifacts:
    - path: "api.py"
      provides: "/api/stops/search endpoint"
  key_links:
    - from: "/api/stops/search"
      to: "PostgreSQL stops table"
      via: "trigram search"
---

<objective>
Implement fuzzy search for stops on the backend and ensure the database supports trigram indexing.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-search-first-entry-time-comparisons/04-CONTEXT.md
@.planning/phases/04-search-first-entry-time-comparisons/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable pg_trgm and add index</name>
  <files>db_init.py</files>
  <read_first>db_init.py</read_first>
  <action>
    Modify db_init.py to:
    1. Run `CREATE EXTENSION IF NOT EXISTS pg_trgm;`
    2. Create a GIST index on `stops.stop_name` for fuzzy matching:
       `CREATE INDEX IF NOT EXISTS stops_name_trgm_idx ON stops USING gist (stop_name gist_trgm_ops);`
  </action>
  <verify>
    <automated>psql -U postgres -d transit -c "\dx" | grep pg_trgm</automated>
  </verify>
  <done>pg_trgm extension enabled and index created</done>
</task>

<task type="auto">
  <name>Task 2: Implement /api/stops/search endpoint</name>
  <files>api.py</files>
  <read_first>api.py</read_first>
  <action>
    Add a new GET route `/api/stops/search` to api.py:
    - Accepts query param `q`.
    - If `q` is a 5-digit number, prioritize exact match on `stop_id`.
    - Otherwise, perform a fuzzy search on `stop_name` using `%` operator.
    - Return a list of `StopInfo` objects.
  </action>
  <verify>
    <automated>curl "http://localhost:8000/api/stops/search?q=Main" | grep "Main"</automated>
  </verify>
  <done>Search API endpoint functional and returns results</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Search query 'q' is untrusted input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | I | /api/stops/search | mitigate | Use parameterized queries with asyncpg to prevent SQL injection |
</threat_model>

<success_criteria>
- Backend supports fuzzy stop search.
- Search API returns stop by ID.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-first-entry-time-comparisons/04-01-SUMMARY.md`
</output>
