---
phase: "04"
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [frontend/package.json, frontend/src/App.tsx, frontend/src/components/Map.tsx, frontend/src/components/HeroSearch.tsx]
autonomous: true
requirements: [SRCH-01, SRCH-05, UIO-01]
must_haves:
  truths:
    - "Map component is gone from the UI"
    - "Hero landing page shows a large search bar"
  artifacts:
    - path: "frontend/src/components/HeroSearch.tsx"
      provides: "New Search UI"
---

<objective>
Remove all map-related dependencies and components, and implement the new Hero Search landing page.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-search-first-entry-time-comparisons/04-CONTEXT.md
@.planning/phases/04-search-first-entry-time-comparisons/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Map components and Leaflet dependencies</name>
  <files>frontend/package.json, frontend/src/App.tsx, frontend/src/components/Map.tsx</files>
  <read_first>frontend/package.json, frontend/src/App.tsx</read_first>
  <action>
    1. Remove `leaflet`, `react-leaflet`, and `@types/leaflet` from package.json.
    2. Delete `frontend/src/components/Map.tsx`.
    3. Remove Map imports and usage from `frontend/src/App.tsx`.
    4. Run `npm install` to update node_modules.
  </action>
  <verify>
    <automated>grep -r "leaflet" frontend/src || echo "No leaflet found"</automated>
  </verify>
  <done>Leaflet and Map components completely removed</done>
</task>

<task type="auto">
  <name>Task 2: Create Hero Search Component</name>
  <files>frontend/src/components/HeroSearch.tsx</files>
  <read_first>frontend/src/components/FilterTable.tsx</read_first>
  <action>
    Create `HeroSearch.tsx` as the main landing UI:
    - Centered large search bar (56px height per UI-SPEC).
    - Auto-suggest dropdown using `/api/stops/search`.
    - "Recent Searches" section below the bar (persisted in localStorage).
    - "Nearby Stops" buttons (mocked for now or using geolocation API).
  </action>
  <verify>
    <automated>test -f frontend/src/components/HeroSearch.tsx</automated>
  </verify>
  <done>Hero Search UI implemented according to specs</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | S | HeroSearch | mitigate | Sanitize search input before sending to API |
</threat_model>

<success_criteria>
- Frontend is map-free.
- Hero search landing page is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-first-entry-time-comparisons/04-02-SUMMARY.md`
</output>
