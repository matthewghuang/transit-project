---
phase: 04-search-first-entry-time-comparisons
plan: 04
type: execute
wave: 1
depends_on: ["04-03"]
files_modified: ["api.py", "frontend/src/components/StopDashboard.tsx", "frontend/src/components/TimeTriad.tsx", "frontend/src/components/DelayDistributionChart.tsx"]
autonomous: true
gap_closure: true
requirements: ["SRCH-01", "SRCH-02", "SRCH-03", "UIO-01"]
must_haves:
  truths:
    - "Searching for a route name (e.g. 'R5') returns stops serving that route"
    - "Searching for a 5-digit stop ID prioritizes the exact match"
    - "Actual and Predicted times are null in API if data is unavailable"
    - "Hero time labels and indicators are cleaner"
    - "Redundant info card is removed from dashboard"
    - "Historical delay distribution (PDF) chart is visible in expanded view"
  artifacts:
    - path: "api.py"
      provides: "Improved search and arrival time logic"
    - path: "frontend/src/components/StopDashboard.tsx"
      provides: "Simplified dashboard UI"
    - path: "frontend/src/components/TimeTriad.tsx"
      provides: "Cleaned up hero time display and fixed chart integration"
    - path: "frontend/src/components/DelayDistributionChart.tsx"
      provides: "Robust rendering of delay probability density"
  key_links:
    - from: "api.py"
      to: "database"
      via: "JOIN with delay_observations and improved ORDER BY"
    - from: "frontend/src/components/TimeTriad.tsx"
      to: "frontend/src/components/DelayDistributionChart.tsx"
      via: "Conditional rendering in expanded state"
---

<objective>
Close UAT gaps in Phase 4:
1. Fix route-based stop searching.
2. Prioritize exact stop ID matches in search results.
3. Remove duplicated/fallback times in API to allow frontend to handle missing data.
4. Clean up UI clutter in StopDashboard and TimeTriad.
5. Fix missing PDF (delay distribution chart) in the expanded view.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api.py
@frontend/src/components/StopDashboard.tsx
@frontend/src/components/TimeTriad.tsx
@frontend/src/components/DelayDistributionChart.tsx
@.planning/phases/04-search-first-entry-time-comparisons/04-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve API Search & Arrival Logic</name>
  <files>api.py</files>
  <action>
    1. Update search_stops:
       - Add a check to see if 'q' matches a route_short_name (fuzzy or exact).
       - Modify the SQL to include stops that serve the matched route.
       - Use a JOIN or subquery with delay_observations (which has route_id) to find stops for a route.
       - Improve ORDER BY for numeric searches to ensure exact stop_id match is always first (weight = 1.0).
    2. Update get_next_buses:
       - Remove fallbacks that set actual_time or predicted_time to scheduled_time.
       - Return null/None if the data is truly missing.
  </action>
  <verify>
    <automated>
      curl -s "http://localhost:8000/api/stops/search?q=R5" | grep -q "stop_id"
      curl -s "http://localhost:8000/api/stops/search?q=50959" | grep -q "\"id\":\"50959\""
    </automated>
  </verify>
  <done>Search returns route-based results and prioritizes IDs; arrival API returns null for missing data.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up Frontend Dashboard & Hero UI</name>
  <files>frontend/src/components/StopDashboard.tsx, frontend/src/components/TimeTriad.tsx</files>
  <action>
    1. StopDashboard.tsx:
       - Remove the "About this stop" info-card section.
    2. TimeTriad.tsx:
       - Simplify the hero-status indicators. Remove the circles/bullets if they feel like "clutter".
       - Ensure that if actual_time or predicted_time are null (from API change), the "Hero Time" logic still functions correctly (falling back to scheduled ONLY for display, but clearly labeled).
       - Ensure the Hero Time label accurately reflects the source.
  </action>
  <verify>
    <automated>
      grep -v "About this stop" frontend/src/components/StopDashboard.tsx
    </automated>
  </verify>
  <done>UI is streamlined and card-free; hero time logic is robust to nulls.</done>
</task>

<task type="auto">
  <name>Task 3: Fix PDF Chart (DelayDistributionChart) Visibility</name>
  <files>frontend/src/components/TimeTriad.tsx, frontend/src/components/DelayDistributionChart.tsx</files>
  <action>
    1. DelayDistributionChart.tsx:
       - Review the component to ensure it handles missing or empty data gracefully (e.g., showing a placeholder or clear message instead of being invisible).
       - Check for any hardcoded height/width constraints that might be collapsing the component to 0px.
    2. TimeTriad.tsx:
       - Verify the conditional rendering logic for `DelayDistributionChart` when expanded.
       - Ensure the necessary data (buckets/probabilities) is correctly passed to the chart component.
       - Wrap the chart in a container with a minimum height to ensure visibility.
  </action>
  <verify>
    <automated>
      grep -l "DelayDistributionChart" frontend/src/components/TimeTriad.tsx
    </automated>
  </verify>
  <done>Delay distribution chart is visible when the time triad is expanded.</done>
</task>

</tasks>

<success_criteria>
- Search for "R5" returns stops.
- Search for "50959" returns that stop as the first result.
- Stop Dashboard no longer contains the "About this stop" card.
- Time Triad UI is simplified.
- Delay distribution chart is visible in the expanded dashboard view.
</success_criteria>
