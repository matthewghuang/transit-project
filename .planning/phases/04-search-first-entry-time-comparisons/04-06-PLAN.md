---
phase: 04
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: ["api.py", "frontend/src/components/HeroSearch.tsx", "frontend/src/components/TimeTriad.tsx", "frontend/src/App.css"]
autonomous: true
requirements: ["GAP-04-01", "GAP-04-02", "GAP-04-03"]
must_haves:
  truths:
    - "Stop ID 50959 displays correctly as #50959 in search results."
    - "Time Triad is visually centered in the dashboard view."
    - "Hero status 'Prediction' text is removed when no real-time data exists."
    - "Actual time column is visible in the expanded Time Triad grid."
  artifacts:
    - path: "api.py"
      provides: "Strict string stop_id handling"
    - path: "frontend/src/components/HeroSearch.tsx"
      provides: "Correct stop_id display"
    - path: "frontend/src/components/TimeTriad.tsx"
      provides: "Restored actual_time column and cleaned hero status"
    - path: "frontend/src/App.css"
      provides: "Centered dashboard layout"
  key_links:
    - from: "api.py"
      to: "PostgreSQL"
      via: "SQL query using string equality for stop_id"
---

<objective>
Fix the remaining UI and data display gaps identified in Phase 4 UAT.
- Ensure stop IDs are handled as strict strings to prevent truncation.
- Polishing the Time Triad layout (centering and status text cleanup).
- Restore missing "Actual" time data in the expanded view.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-search-first-entry-time-comparisons/04-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strict String Stop ID Handling</name>
  <files>api.py, frontend/src/components/HeroSearch.tsx</files>
  <action>
    - In api.py: Ensure search_stops handles $1 as a string and doesn't perform implicit casts that might truncate leading characters if the DB type is VARCHAR (which it should be). The logic `is_numeric = q.isdigit() and (len(q) >= 4 and len(q) <= 6)` is fine for branching, but ensure the SQL `s.stop_id = $1` is executed against the raw string.
    - In HeroSearch.tsx: Verify the `stop-id` span is rendering `stop.id` directly. The gap suggests "#959" instead of "#50959", which likely means the backend returned "959".
  </action>
  <verify>
    <automated>curl -s "http://localhost:8000/api/stops/search?q=50959" | grep -q "50959"</automated>
  </verify>
  <done>Search for 50959 returns the correct full ID string.</done>
</task>

<task type="auto">
  <name>Task 2: Time Triad Polishing & Status Cleanup</name>
  <files>frontend/src/components/TimeTriad.tsx, frontend/src/App.css</files>
  <action>
    - In TimeTriad.tsx:
        - Remove the `hero-status` div or modify logic so it doesn't show "Prediction" when falling back to predicted time (per UAT requirement "final removal of prediction subtitle").
        - Restore "Actual" time in `triad-grid`. Currently, `times.map` iterates over all labels, but check if `actual_time` is being blanked out by the hook or the display logic.
    - In App.css:
        - Update `.dashboard-main` or `.triad-section` to ensure the `.time-triad` container is horizontally centered. Use `margin: 0 auto` and `display: flex; flex-direction: column; align-items: center;`.
  </action>
  <verify>
    <automated>grep -v "hero-status" frontend/src/components/TimeTriad.tsx || grep -c "hero-status" frontend/src/components/TimeTriad.tsx</automated>
  </verify>
  <done>Time Triad is centered, "Prediction" subtitle is gone, and Actual time column is visible when expanded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Frontend -> API | Search query string is untrusted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Injection | api.py (search) | mitigate | Use parameterized queries (asyncpg $1) for stop_id search |
</threat_model>

<verification>
1. Search for "50959" in the UI and verify "#50959" is displayed.
2. Select a stop and verify the Time Triad is centered.
3. Verify no "Prediction" subtitle appears under the large time.
4. Click to expand and verify "Actual" column shows a time or "--:--:--".
</verification>

<success_criteria>
- UAT Gap 1 (Stop ID truncation) resolved.
- UAT Gap 2 (Centering & Subtitle) resolved.
- UAT Gap 3 (Actual time missing) resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-first-entry-time-comparisons/04-06-SUMMARY.md`
</output>
