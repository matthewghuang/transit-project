---
phase: 04-search-first
plan: 09
type: execute
wave: 1
depends_on: ["04-08"]
files_modified: ["api.py", "frontend/src/components/HeroSearch.tsx", "frontend/src/components/StopDashboard.tsx"]
autonomous: true
requirements: ["SRCH-01", "SRCH-04"]
gap_closure: true

must_haves:
  truths:
    - "Search results return the 5-digit stop code as the primary ID"
    - "Stop Dashboard displays the 5-digit stop code in the header"
    - "API endpoints resolve stop codes to internal IDs for data lookup while returning the code in the response"
  artifacts:
    - path: "api.py"
      provides: "Stop code resolution and ID mapping"
    - path: "frontend/src/components/HeroSearch.tsx"
      provides: "Search results with stop codes"
  key_links:
    - from: "HeroSearch.tsx"
      to: "/api/stops/search"
      via: "HTTP GET"
    - from: "api.py"
      to: "stops_lookup"
      via: "Dictionary lookup"
---

<objective>
Fix the "Conflicting IDs" issue where the system uses internal GTFS stop_id for display instead of the visible stop_code.

Purpose: Ensure users see and use the familiar 5-digit stop codes they see at bus stops, while the backend maintains data integrity using internal IDs.
Output: Consistent use of stop_code for display and communication across API and Frontend.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/debug/stop-code-id-conflict.md
@api.py
@frontend/src/components/HeroSearch.tsx
@frontend/src/components/StopDashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Backend to Prefer stop_code for Display</name>
  <files>api.py</files>
  <action>
    - Update `load_stops()` to build an `id_to_stop_code` mapping.
    - Modify `search_stops` SQL query to select `stop_code` from the `stops` table.
    - In `search_stops`, populate the `id` field of `StopInfo` with `stop_code` if available, falling back to `stop_id`.
    - Update `get_stops` to also return `stop_code` as the `id`.
    - Ensure `get_delay_distribution` and `get_next_buses` continue to resolve the input `stop_id` (which may now be a code) using `stop_code_to_id`, but return the input `stop_id` back in the response to maintain frontend state consistency.
  </action>
  <verify>
    <automated>curl -s "http://localhost:8000/api/stops/search?q=50959" | grep -q '"id":"50959"'</automated>
  </verify>
  <done>Backend returns stop_code in the 'id' field for search and stop listings.</done>
</task>

<task type="auto">
  <name>Task 2: Align Frontend with stop_code IDs</name>
  <files>frontend/src/components/HeroSearch.tsx, frontend/src/components/StopDashboard.tsx</files>
  <behavior>
    - HeroSearch: Displays the ID returned from the API (now stop_code) prefixed with '#'.
    - StopDashboard: Displays the ID passed from HeroSearch (now stop_code) in the header.
  </behavior>
  <action>
    - In `HeroSearch.tsx`, verify the `StopResult` interface aligns with `StopInfo` (it already has `id: string`).
    - In `StopDashboard.tsx`, the header already uses `stopId`. Since the API now provides the code as the `id`, no functional change is needed other than verifying the display logic.
  </action>
  <verify>
    <automated>grep -C 5 "Stop #{" frontend/src/components/StopDashboard.tsx</automated>
  </verify>
  <done>Frontend displays the 5-digit stop code in search results and dashboard header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | User-provided stop IDs (codes or internal IDs) are validated and resolved on the backend. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Information Disclosure | api.py | mitigate | Ensure internal stop_ids are not leaked if stop_code is available, but maintain fallback for stops without codes. |
</threat_model>

<verification>
1. Start the API: `uv run python api.py`
2. Search for a known stop by code: `curl http://localhost:8000/api/stops/search?q=50959`
3. Verify the `id` in the response is "50959".
4. Check that `/api/distribution/50959` returns valid data using the code.
</verification>

<success_criteria>
- Backend API returns stop_code as the primary 'id' for stops.
- Frontend displays the 5-digit code in the dashboard header.
- Search results show the 5-digit code.
</success_criteria>

<output>
After completion, create .planning/phases/04-search-first/04-09-SUMMARY.md
</output>
