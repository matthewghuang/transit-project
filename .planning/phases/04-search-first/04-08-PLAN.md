---
phase: 04-search-first
plan: 08
type: execute
wave: 1
depends_on: ["04-07"]
files_modified: ["api.py"]
autonomous: true
requirements: ["SRCH-04", "UIO-01"]
gap_closure: true

must_haves:
  truths:
    - "API correctly resolves stop_code to stop_id for next bus lookups"
    - "Dashboard displays actual times for stops searched by their 5-digit code"
  artifacts:
    - path: "api.py"
      provides: "stop_code to stop_id mapping and resolution logic"
  key_links:
    - from: "get_next_buses"
      to: "stop_code_to_id lookup"
      via: "variable assignment"
---

<objective>
Fix the "--:--:--" display issue caused by ID mismatch between stop_code and stop_id.

Purpose: Ensure real-time arrival data is correctly retrieved even when users search for a stop using its 5-digit code.
Output: Updated api.py with an in-memory stop_code mapping and resolution logic in the get_next_buses endpoint.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api.py
@google_transit/stops.txt
@.planning/debug/actual-time-placeholder-display.md

<interfaces>
<!-- Key types and contracts the executor needs. -->
class NextBusesResponse(BaseModel):
    stop_id: str
    scheduled_time: Optional[str] = None
    actual_time: Optional[str] = None
    predicted_time: Optional[str] = None

@app.get("/api/stops/{stop_id}/next_buses", response_model=NextBusesResponse)
async def get_next_buses(stop_id: str):
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement stop_code_to_id mapping</name>
  <files>api.py</files>
  <action>
    1. Define a global dictionary `stop_code_to_id: Dict[str, str] = {}`.
    2. Update the `load_stops()` function to populate this dictionary from `stops.txt`. Map `row["stop_code"]` to `row["stop_id"]`.
    3. Ensure `load_stops()` handles missing or empty `stop_code` values gracefully.
  </action>
  <verify>
    <automated>grep "stop_code_to_id" api.py</automated>
  </verify>
  <done>stop_code_to_id mapping is populated during startup.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve stop_id in get_next_buses</name>
  <files>api.py</files>
  <action>
    1. Modify the `get_next_buses` endpoint.
    2. At the beginning of the function, check if the provided `stop_id` exists as a key in `stop_code_to_id`.
    3. If it does, update the local `stop_id` variable to the resolved `stop_id` from the mapping.
    4. Also update `get_delay_distribution` to perform the same resolution to ensure historical data is correctly fetched for stop codes.
  </action>
  <verify>
    <automated>python -c "import api; print('Resolution logic added')"</automated>
  </verify>
  <done>Endpoints correctly resolve stop_code to stop_id before performing lookups.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | stop_id parameter is user-provided |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Information Disclosure | get_next_buses | mitigate | Validate that resolved stop_id exists in GTFS metadata before proceeding with lookups. |
</threat_model>

<verification>
1. Start the API: `uv run uvicorn api:app --reload`
2. Test resolution: `curl http://localhost:8000/api/stops/50959/next_buses`
3. Verify `scheduled_time` is not null and matches GTFS for stop_id 968.
</verification>

<success_criteria>
- The API accepts both stop_id (e.g., 968) and stop_code (e.g., 50959).
- Next bus lookups for stop_codes return valid scheduled and actual times.
- No regression in stop_id lookups.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-first/04-08-SUMMARY.md`
</output>
