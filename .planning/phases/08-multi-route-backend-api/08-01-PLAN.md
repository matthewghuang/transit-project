---
phase: "08"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [api.py]
autonomous: true
requirements: [MULT-01]
must_haves:
  truths:
    - "/api/stops/{id}/next_buses returns an array of arrivals instead of a single object"
    - "Each arrival in the array represents a unique route at that stop"
    - "Arrivals are sorted chronologically by scheduled_time"
    - "The Time Triad (Scheduled, Actual, Predicted, Arrive-by) is present for each route"
  artifacts:
    - path: "api.py"
      provides: "Multi-route arrival API endpoint"
  key_links:
    - from: "NextBusesResponse"
      to: "/api/stops/{stop_id}/next_buses"
      via: "response_model=List[NextBusesResponse]"
---

<objective>
Update the `/api/stops/{stop_id}/next_buses` endpoint to return a list of next arrivals for every unique route serving the stop.

Purpose: Support the multi-route carousel UI in Phase 9 by providing data for all relevant buses at a stop in a single request.
Output: Refactored `api.py` with multi-route support in memory and in the API response.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@api.py
@google_transit/stop_times.txt
@google_transit/trips.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich GTFS lookups with route_id</name>
  <files>api.py</files>
  <action>
    - Update `load_stop_times()` to also read `google_transit/trips.txt` and build a `trip_to_route` mapping.
    - Modify `stop_times_lookup` storage to include `route_id` for each entry: `stop_times_lookup[sid].append((sec, arr_str, trip_id, route_id))`.
    - Update `NextBusesResponse` Pydantic model to include `route_id` and `route_name` fields.
  </action>
  <verify>
    <automated>python -c "from api import load_stops, load_routes, load_stop_times, stop_times_lookup; load_stops(); load_routes(); load_stop_times(); print(next(iter(stop_times_lookup.values()))[0])" | grep -E "'.*'"</automated>
  </verify>
  <done>GTFS lookups in memory now associate stop times with their respective route IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor API to return unique route arrivals</name>
  <files>api.py</files>
  <action>
    - Modify `@app.get("/api/stops/{stop_id}/next_buses")` response model to `List[NextBusesResponse]`.
    - Update the endpoint logic to:
      1. Iterate through `stop_times_lookup[stop_id]` starting from `now_sec`.
      2. Collect the *first* upcoming arrival for each unique `route_id`.
      3. For each collected arrival, perform the existing real-time and historical calculations (Time Triad).
      4. Return the list of arrivals, sorted by `scheduled_time`.
    - Ensure `route_id` and `route_name` are populated in the response.
  </action>
  <verify>
    <automated>curl -s "http://localhost:8000/api/stops/50043/next_buses" | jq 'if type == "array" then true else false end'</automated>
  </verify>
  <done>API returns an array of unique route arrivals, sorted by time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted stop_id and confidence parameters |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | I | /api/stops/{id}/next_buses | mitigate | Validate stop_id existence; snap confidence to discrete steps |
</threat_model>

<verification>
1. Restart the API: `uv run python api.py` (if running manually) or check logs if running in docker.
2. Search for a stop known to have multiple routes (e.g., a major exchange).
3. Call the `next_buses` endpoint for that stop.
4. Verify the response is a JSON array.
5. Verify `route_id` and `route_name` are present and unique within the array.
6. Verify sorting is chronological.
</verification>

<success_criteria>
- `/api/stops/{id}/next_buses` returns `List[NextBusesResponse]`.
- Every unique route at the stop has at most one arrival in the list (the next one).
- Sorting is by `scheduled_time` ascending.
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-route-backend-api/08-01-SUMMARY.md`
</output>
