---
phase: 06-dynamic-percentile-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api.py]
autonomous: true
requirements: [CORE-04, CORE-05]
must_haves:
  truths:
    - "API accepts confidence parameter (50, 75, 90, 95, 99)"
    - "API returns dynamic percentile delay bounds based on confidence"
    - "Arrive-by recommendation is never later than scheduled time"
    - "Response includes low_confidence flag if <10 observations"
  artifacts:
    - path: "api.py"
      provides: "Dynamic percentile API endpoints"
  key_links:
    - from: "/api/distribution/{stop_id}"
      to: "TimescaleDB percentile_agg"
      via: "SQL query"
      pattern: "approx_percentile"
---

<objective>
Update the backend API to support dynamic confidence percentiles and enforce arrive-by safety logic.

Purpose: Allow users to customize their risk tolerance and ensure they never miss a bus due to over-optimistic delay predictions.
Output: Updated api.py with dynamic distribution and safety-capped arrive-by times.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/turq/code/transit-data/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dynamic-percentile-backend/06-CONTEXT.md
@.planning/phases/06-dynamic-percentile-backend/06-RESEARCH.md
@api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Pydantic models and add snapping utility</name>
  <files>api.py</files>
  <read_first>api.py</read_first>
  <action>
    - Add `confidence: float = 95.0` and `low_confidence: bool = False` to `DistributionResponse` and `NextBusesResponse`.
    - Add a helper function `snap_percentile(p: float) -> float` that maps input confidence to the nearest value in [0.5, 0.75, 0.90, 0.95, 0.99].
  </action>
  <verify>
    <automated>grep "low_confidence" api.py && grep "snap_percentile" api.py</automated>
  </verify>
  <done>Models updated with confidence fields and helper function exists.</done>
</task>

<task type="auto">
  <name>Task 2: Update distribution endpoint to use TimescaleDB hyperfunctions</name>
  <files>api.py</files>
  <read_first>api.py</read_first>
  <action>
    - Update `@app.get("/api/distribution/{stop_id}")` to accept a `confidence: float = 95.0` query parameter.
    - Snap the input confidence using `snap_percentile`.
    - Replace the `numpy` in-memory histogram and percentile logic with a single TimescaleDB SQL query using `percentile_agg` and `approx_percentile`.
    - Set `low_confidence = True` if the row count is less than 10 (per D-04).
  </action>
  <verify>
    <automated>grep "approx_percentile" api.py && grep "confidence" api.py</automated>
  </verify>
  <done>Distribution endpoint returns dynamic percentiles calculated in-DB.</done>
</task>

<task type="auto">
  <name>Task 3: Update next_buses endpoint with arrive-by safety logic</name>
  <files>api.py</files>
  <read_first>api.py</read_first>
  <action>
    - Update `@app.get("/api/stops/{stop_id}/next_buses")` to accept a `confidence: float = 95.0` query parameter.
    - Snap the input confidence.
    - Calculate the percentile delay using `approx_percentile` in the historical query.
    - Apply Arrive-By safety cap (D-05): `arrive_by_sec = min(sched_sec, sched_sec + percentile_delay_sec)`.
    - Include `low_confidence` flag in the response.
  </action>
  <verify>
    <automated>grep "min(sched_sec" api.py</automated>
  </verify>
  <done>Next buses endpoint enforces Arrive-By safety cap and supports dynamic confidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Confidence parameter is untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I | api.py | mitigate | Use `snap_percentile` to enforce valid discrete steps and prevent SQL injection or out-of-bounds calculations |
| T-06-02 | D | api.py | mitigate | Approximate aggregates (percentile_agg) prevent high-memory sort operations on large stop datasets |
</threat_model>

<verification>
Run `python api.py` and test with curl:
`curl "http://localhost:8000/api/distribution/50240?confidence=75"`
Check if `confidence` is returned as 75.0 and `arrive_by_time` is correctly capped.
</verification>

<success_criteria>
- API accepts dynamic confidence param.
- Arrive-by recommendation is <= scheduled time.
- Low data warning appears when observations < 10.
</success_criteria>

<output>
After completion, create .planning/phases/06-dynamic-percentile-backend/06-01-SUMMARY.md
</output>
