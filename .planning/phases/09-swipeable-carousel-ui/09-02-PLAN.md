---
phase: "09"
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: ["frontend/package.json", "frontend/src/components/Carousel.tsx", "frontend/src/components/StopDashboard.tsx", "frontend/src/App.css"]
autonomous: true
requirements: ["MULT-02", "MULT-03"]
must_haves:
  truths:
    - "Multiple arrival cards are displayed in a horizontal carousel"
    - "The carousel supports swiping on mobile devices"
    - "Navigation arrows are visible on desktop view"
    - "Carousel shows partial peek of the next card"
  artifacts:
    - path: "frontend/src/components/Carousel.tsx"
      provides: "Embla-based carousel wrapper"
  key_links:
    - from: "frontend/src/components/StopDashboard.tsx"
      to: "frontend/src/components/Carousel.tsx"
      via: "React Component"
---

<objective>
Implement the Embla Carousel to allow users to swipe between different bus arrivals at a single stop.

Purpose: Provide mobile-friendly navigation for multi-route stops.
Output: Swipeable horizontal carousel with partial peek and desktop navigation.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/turq/code/transit-data/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/StopDashboard.tsx
@frontend/src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Embla and Create Carousel Component</name>
  <files>frontend/package.json, frontend/src/components/Carousel.tsx</files>
  <action>
    Install `embla-carousel-react` using `npm install` in the `frontend` directory.
    Create a `Carousel.tsx` component that wraps the `useEmblaCarousel` hook.
    It should take `children: React.ReactNode[]` as a prop and render each child in an embla slide.
    Include a basic viewport and container structure as recommended by Embla docs (per D-01).
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>Carousel component created and Embla installed.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Carousel into StopDashboard</name>
  <files>frontend/src/components/StopDashboard.tsx, frontend/src/App.css</files>
  <action>
    Replace the single `TimeTriad` in `StopDashboard` with the `Carousel` component.
    Map over the `arrivals` array fetched by `useNextBuses` and render a `TimeTriad` for each (per D-06).
    Ensure the `StopDashboard` layout doesn't break with the horizontal overflow.
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>All Arrivals are rendered within the Carousel.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Partial Peek and Desktop Navigation</name>
  <files>frontend/src/App.css, frontend/src/components/Carousel.tsx</files>
  <action>
    Implement CSS for the "Partial Peek" effect: the embla container should allow slides to take up ~85% width so the next slide is partially visible (per D-02).
    Add navigation arrows (Prev/Next) to the `Carousel` component that are only visible on hover or on desktop screens (per D-03).
    Use the Embla API (`scrollPrev`, `scrollNext`) to wire the arrows.
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>Carousel looks and feels like a mobile-first slider with desktop affordances.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Layout | Cross-browser compatibility of carousel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | D | Carousel.tsx | mitigate | Ensure carousel doesn't crash on zero items |
| T-09-04 | D | Carousel.tsx | mitigate | Prevent horizontal scroll of entire page by containing the carousel viewport |
</threat_model>

<verification>
Check for console errors when swiping or clicking arrows.
</verification>

<success_criteria>
- Horizontal carousel is functional.
- Partial peek is visible.
- Swiping works.
</success_criteria>

<output>
After completion, create `.planning/phases/09-swipeable-carousel-ui/09-02-SUMMARY.md`
</output>
