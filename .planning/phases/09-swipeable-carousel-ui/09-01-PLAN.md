---
phase: "09"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["frontend/src/hooks/useNextBuses.ts", "frontend/src/components/TimeTriad.tsx", "frontend/src/components/StopDashboard.tsx"]
autonomous: true
requirements: ["MULT-02"]
must_haves:
  truths:
    - "TimeTriad is a stateless component receiving data via props"
    - "StopDashboard orchestrates fetching of multi-route arrival data"
    - "The UI still displays the first arrival correctly after refactoring"
  artifacts:
    - path: "frontend/src/hooks/useNextBuses.ts"
      provides: "Updated arrival interface and list-based fetching"
    - path: "frontend/src/components/TimeTriad.tsx"
      provides: "Stateless arrival card component"
  key_links:
    - from: "frontend/src/components/StopDashboard.tsx"
      to: "frontend/src/hooks/useNextBuses.ts"
      via: "useNextBuses hook"
---

<objective>
Refactor the frontend data flow to support multiple arrivals per stop. This involves updating the arrival hook to handle the new list-based API response and making the TimeTriad component stateless so it can be reused in a carousel.

Purpose: Prepare the codebase for the carousel implementation by decoupling data fetching from display.
Output: Refactored components and hooks supporting multi-route data.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/turq/code/transit-data/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/TimeTriad.tsx
@frontend/src/components/StopDashboard.tsx
@frontend/src/hooks/useNextBuses.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update useNextBuses Hook</name>
  <files>frontend/src/hooks/useNextBuses.ts</files>
  <action>
    Update the `NextBusesResponse` interface to match the backend refactor in Phase 08.
    Add `route_id` and `route_name` (string | null) to the interface.
    Update the `useNextBuses` hook's return type to reflect that it now returns an array of arrivals (`NextBusesResponse[]`).
    Ensure the `queryKey` remains stable.
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>Hook returns a list of arrivals and interfaces match backend.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TimeTriad to be Prop-based</name>
  <files>frontend/src/components/TimeTriad.tsx</files>
  <action>
    Modify `TimeTriadProps` to accept an `arrival: NextBusesResponse` object instead of fetching it internally.
    Remove the `useNextBuses` call from within `TimeTriad`.
    Keep the `useQuery` for distribution data (this is still per-stop).
    Update the component to use the passed `arrival` prop for all display logic (scheduled, actual, predicted times, staleness, etc.).
    Add the `route_name` to the card display (e.g., above the hero time).
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>TimeTriad is stateless regarding arrival data and ready for carousel reuse (per D-05).</done>
</task>

<task type="auto">
  <name>Task 3: Update StopDashboard Orchestration</name>
  <files>frontend/src/components/StopDashboard.tsx</files>
  <action>
    Import and use `useNextBuses` in `StopDashboard`.
    Pass the first arrival from the resulting data array to the `TimeTriad` component as a temporary placeholder.
    Add loading and error states to `StopDashboard` since it now handles the primary data fetch (per D-06).
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>StopDashboard successfully fetches multi-arrival data and passes it to the refactored TimeTriad.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Frontend | Untrusted JSON data from backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | I | useNextBuses.ts | mitigate | Validate that data is an array before mapping in UI |
| T-09-02 | D | StopDashboard.tsx | mitigate | Handle empty array response from API gracefully |
</threat_model>

<verification>
Check that the dashboard still loads and shows the first bus arrival for a stop correctly.
</verification>

<success_criteria>
- useNextBuses hook returns Array of arrivals.
- TimeTriad is refactored to accept props.
- StopDashboard displays the first arrival.
</success_criteria>

<output>
After completion, create `.planning/phases/09-swipeable-carousel-ui/09-01-SUMMARY.md`
</output>
