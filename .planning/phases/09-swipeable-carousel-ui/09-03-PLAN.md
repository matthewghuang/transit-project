---
phase: "09"
plan: "03"
type: execute
wave: 3
depends_on: ["09-02"]
files_modified: ["frontend/src/components/StopDashboard.tsx", "frontend/src/components/TimeTriad.tsx", "frontend/src/App.css"]
autonomous: false
requirements: ["MULT-03"]
must_haves:
  truths:
    - "A single confidence slider affects all arrival cards in the carousel"
    - "Cards no longer contain individual confidence sliders"
    - "Visual styling of dots/arrows is consistent and clear"
    - "Expanded states of cards are handled gracefully"
  artifacts:
    - path: "frontend/src/components/StopDashboard.tsx"
      provides: "Global confidence control"
  key_links:
    - from: "frontend/src/components/StopDashboard.tsx"
      to: "frontend/src/stores/filterStore.ts"
      via: "setConfidenceLevel"
---

<objective>
Finalize the UI by implementing the global confidence slider and polishing the visual experience.

Purpose: Synchronize controls across all arrivals and ensure a cohesive look and feel.
Output: Integrated Global Slider and polished UX with a checkpoint for visual verification.
</objective>

<execution_context>
@/Users/turq/code/transit-data/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/turq/code/transit-data/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/StopDashboard.tsx
@frontend/src/components/TimeTriad.tsx
@frontend/src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Global Confidence Slider</name>
  <files>frontend/src/components/StopDashboard.tsx, frontend/src/components/TimeTriad.tsx</files>
  <action>
    Remove the `reliability-slider` section from `TimeTriad.tsx` (per D-04).
    Implement the `reliability-slider` in `StopDashboard.tsx`, positioned above the Carousel.
    Ensure the slider uses `useFilterStore` to get and set the global `confidenceLevel`.
    Ensure all `TimeTriad` cards update their "Arrive-by" recommendation instantly when the global slider moves.
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>Confidence slider is centralized and global.</done>
</task>

<task type="auto">
  <name>Task 2: UI Visual Polish & Height Handling</name>
  <files>frontend/src/App.css, frontend/src/components/Carousel.tsx</files>
  <action>
    Style the Embla navigation dots and arrows for a "swish" professional look (per D-07).
    Adjust `TimeTriad` card heights to ensure they are consistent within the carousel.
    If a card is expanded, handle the height change (either allow the whole carousel to grow, or use a scrollable inner area).
    Implement D-08's discretion: keep the expanded state within the card but ensure the carousel height doesn't cause layout jumps.
  </action>
  <verify>
    <automated>npm run build --prefix frontend</automated>
  </verify>
  <done>Visual styling is complete and cards behave well when expanded.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual & Functional Verification</name>
  <files>N/A</files>
  <action>Perform manual verification of the carousel UI on different screen sizes.</action>
  <verify>
    <automated>MISSING — Manual verification required</automated>
  </verify>
  <done>UI is verified and approved by the user.</done>
  <what-built>Mobile-first swipeable carousel with global confidence control.</what-built>
  <how-to-verify>
    1. Start the frontend (npm start in frontend folder).
    2. Search for a stop with multiple routes (e.g. Broadway & Cambie).
    3. Verify you can swipe through multiple cards on mobile emulation.
    4. Verify arrows work on desktop.
    5. Slide the global confidence slider and watch all cards update their arrive-by times.
    6. Expand a card and check that the layout remains stable.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Layout | Overflow issues on small screens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | D | StopDashboard.tsx | mitigate | Test on iPhone SE size to ensure the slider and carousel don't overlap or fall off screen |
| T-09-06 | D | StopDashboard.tsx | mitigate | Ensure the back button is always clickable regardless of carousel state |
</threat_model>

<verification>
Human approval of the mobile-first UX.
</verification>

<success_criteria>
- Single slider affects all cards.
- Mobile swiping works smoothly.
- Desktop arrows work smoothly.
- Partial peek effect is clear.
</success_criteria>

<output>
After completion, create `.planning/phases/09-swipeable-carousel-ui/09-03-SUMMARY.md`
</output>
