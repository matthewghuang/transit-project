---
phase: quick
plan: 260410-gg2
type: execute
wave: 1
depends_on: []
files_modified: [start.sh]
autonomous: true
requirements: [START-SCRIPT]
must_haves:
  truths:
    - "A single script can start the infrastructure, producer, consumer, API, and frontend"
  artifacts:
    - path: "start.sh"
      provides: "Unified startup script"
---

<objective>
Create a unified `start.sh` script to streamline the development environment startup. This script will orchestrate Docker infrastructure and all Python/JS services (producer, consumer, API, frontend).
</objective>

<execution_context>
@/Users/turq/code/transit-data/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@main.py
@delay_consumer.py
@api.py
@frontend/package.json
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create start.sh</name>
  <files>start.sh</files>
  <action>
    Create a bash script `start.sh` that:
    1. Starts Docker infrastructure (`docker-compose up -d`).
    2. Waits for TimescaleDB and Kafka to be ready (brief sleep or wait-for-it).
    3. Starts the backend services in the background using `uv run` (or direct python if uv is not available, but project uses uv):
       - `python main.py` (producer)
       - `python delay_consumer.py` (consumer)
       - `fastapi run api.py` (API - per FastAPI 0.120+ conventions or `uvicorn api:app`)
    4. Starts the frontend in the background:
       - `cd frontend && npm start`
    5. Traps SIGINT to kill all background processes on Ctrl+C.
  </action>
  <verify>
    <automated>bash -n start.sh</automated>
  </verify>
  <done>start.sh created and executable</done>
</task>

</tasks>

<success_criteria>
- `start.sh` exists in the root directory.
- `start.sh` is executable.
- Script handles process cleanup on exit.
</success_criteria>

<output>
After completion, create `.planning/quick/260410-gg2-create-a-start-script-to-start-the-produ/260410-gg2-SUMMARY.md`
</output>
